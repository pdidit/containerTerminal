apiVersion: v1
kind: Service
metadata:
  name: restapi
  labels:
    apps: restapi
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      name: restapi
  selector:
    app: restapi